# team_display
Webapp auf dem Display

Virtuellle Umgebung ertsellen:
python -m venv venv
venv\Scripts\activate

Abhängigkeiten installieren:
pip install -r requirements.txt

Anwendung starten:
python3 app.py